# Frontend env — copy to `.env` for local dev overrides.
# Vite exposes only vars prefixed with VITE_ to the client bundle.

# Backend REST API base URL. Default assumes backend on same host, port 3000.
# Production behind nginx: VITE_API_BASE_URL=/api
VITE_API_BASE_URL=http://localhost:3000/api
